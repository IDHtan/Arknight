# Arknight 项目架构文档

## 0. 总览

当前项目建议从“是否属于长期保留资产”来理解，而不是只看功能。

### 0.1 核心层

1. 总状态层：`URougeliteRunSubsystem`、`URougeliteSaveGame`、`URougeliteSettings`
2. 数据描述层：`DS.h` 中的枚举与 `USTRUCT`

### 0.2 BattleMap 原型的非总状态层

1. `BattleMap/Controller`：战斗关卡驱动、输入、镜头、局内运行态控制与本场资源统计入口
2. `BattleMap`：网格地图生成与格子管理
3. `BattleMap/Cell`：地块对象
4. `BattleMap/Data`：战斗期临时数据对象
5. `BattleMap/UI`：战斗 HUD、卡片、详情、部署面板
6. `Component`：生命与索敌组件
7. `Operator`：干员与子弹
8. `Interface`：索敌/受击接口
9. `Debug`：调试入口
10. 模块入口：`Arknight.cpp`

---

## 1. 总状态层

### **[URougeliteRunSubsystem] (继承自 [UGameInstanceSubsystem])**

职责边界：整个项目的运行态总管。负责读取/保存存档、维护当前局外资源与干员等级缓存、装配 `GlobalRoster`，并在关卡间传递“即将进入哪张战斗地图”的凭证。

Tech Note：这是当前项目最值得保留的核心系统。即使以后不用 BattleMap，HexMap 方案也大概率仍然需要一个常驻 `UGameInstanceSubsystem` 来承接 Meta 数据、局内缓存和跨关卡跳转参数。

Properties (局外/硬盘映射):

```cpp
UPROPERTY()
URougeliteSaveGame* CurrentSaveGame; // 当前内存中的存档实例，所有资源与等级修改最终都会落到这里

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "System|SaveLoad")
FString SaveSlotName = TEXT("MainSaveSlot"); // 存档槽名，当前固定单存档
```

Properties (局内/内存暂存):

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "RunData|Flow")
FName PendingBattleLevelID; // 待进入战斗关卡的逻辑 ID，当前主要由调试模式写入

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "RunData|Economy")
int32 CurrentAP; // 行动力/局内资源入口，当前代码里尚未完整展开

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Roster")
TArray<FOperatorRosterData> GlobalRoster; // 基于存档等级 + 配置表组装出来的全局干员清单
```

Methods (底层系统/存取流转):

```cpp
virtual void Initialize(FSubsystemCollectionBase& Collection) override; // 引擎创建子系统时自动调用；内部依次执行 LoadGame() 和 CheckOperators()，把存档与配置表组装成可用运行态
virtual void Deinitialize() override; // 引擎销毁子系统时自动调用；内部执行 SaveGame() 作为兜底保存

UFUNCTION(BlueprintCallable, Category = "System|SaveLoad")
void LoadGame(); // 从硬盘读档；若槽位不存在则创建新的 URougeliteSaveGame 实例

UFUNCTION(BlueprintCallable, Category = "System|SaveLoad")
void SaveGame(); // 把 CurrentSaveGame 覆写保存到硬盘；适合在升级、结算、退出等关键节点调用

UFUNCTION(BlueprintCallable, Category = "System|SaveLoad")
void CheckOperators(); // 读取 OperatorDictTable，把新干员补进存档等级表，并重建 GlobalRoster；这是“配置表 -> 运行态 roster”的装配入口
```

Methods (局外 Meta 业务):

```cpp
UFUNCTION(BlueprintCallable, Category = "MetaProgression")
void UpgradeOperator(FName OperatorName); // 提升干员等级，同时同步更新 CurrentSaveGame 与 GlobalRoster；当前实现尚未自动 SaveGame()
```

Methods (局内 Run 业务):

```cpp
UFUNCTION(BlueprintCallable, Category = "RunLogic")
void PrepareForBattle(FName LevelID); // 当前实现是直接 OpenLevel(LevelID)；按设计它未来更适合作为“写入 PendingBattleLevelID 后切场景”的统一入口

UFUNCTION(BlueprintCallable, Category = "RunLogic")
FName ConsumePendingBattleID(); // 读取并清空 PendingBattleLevelID；应由战斗 GameMode/地图生成入口消费

UFUNCTION(BlueprintCallable, Category = "RunLogic")
void AddResource(EResourceType Type, int32 Amount); // 直接把采集到的资源写入 CurrentSaveGame->GlobalResources

UFUNCTION(BlueprintCallable, Category = "RunLogic")
void ConcludeBattle(); // 战斗结算占位函数；当前尚未实现，是后续必须补完的总流程节点
```

当前实现提醒：

- `PrepareForBattle()` 当前没有写 `PendingBattleLevelID`
- `PendingBattleLevelID` 目前主要由 `ALvlDebugGameMode` 直接写入
- `UpgradeOperator()` 当前没有自动触发 `SaveGame()`
- `ConcludeBattle()` 当前为空

### **[URougeliteSaveGame] (继承自 [USaveGame])**

职责边界：局外永久数据容器。负责承载资源、干员等级和玩家等级。

Tech Note：这是“纯数据类”。真正的业务入口不应该直接散落在 UI 或关卡里，而应该尽量经由 `URougeliteRunSubsystem` 操作它。

Properties:

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadWrite)
TMap<EResourceType, int32> GlobalResources; // 全局资源背包，采集与结算时应读写这里

UPROPERTY(VisibleAnywhere, BlueprintReadWrite)
TMap<FName, int32> OperatorLevels; // 干员等级字典，CheckOperators/UpgradeOperator 时应读写这里

UPROPERTY(VisibleAnywhere, BlueprintReadWrite)
int32 PlayerLevel = 1; // 玩家整体账号/进度等级，当前尚未被更多逻辑使用
```

Methods:

- 无自定义方法；本类只承担序列化容器职责

### **[URougeliteSettings] (继承自 [UDeveloperSettings])**

职责边界：项目级数据表与配置入口。

Tech Note：只要项目还保留“DataTable 驱动干员/关卡配置”的模式，这个类就值得继续保留。后续 HexMap 即便替换战斗对象，也仍可能继续读这里的 DataTable。

Properties:

```cpp
UPROPERTY(Config, EditAnywhere, Category = "DataTables")
TSoftObjectPtr<UDataTable> OperatorDictTable; // 干员字典表；应在 CheckOperators() 或任何需要把名字映射到干员类时读取

UPROPERTY(Config, EditAnywhere, Category = "DataTables")
TSoftObjectPtr<UDataTable> LevelLayoutTable; // 关卡布局表；当前 BattleMapManager 构图时读取，未来 HexMap 可替换为新的地图配置表
```

关键资产读取时机：

- `OperatorDictTable`：启动后构建 `GlobalRoster` 时读取；不要每次点卡片都重新同步加载
- `LevelLayoutTable`：进入某一战斗地图并需要实际生成格子时读取；如果未来不再使用 BattleMap，这个资产将是首批被替换的对象

Methods:

- 无自定义方法；职责是“提供可编辑的配置引用”

---

## 2. 数据描述层

### 文件：[DS.h]

职责边界：项目的统一枚举与轻量数据结构集合，负责给总状态层、战斗层、UI 层提供一致的数据语言。

Tech Note：这是当前项目最适合继续保留的第二批内容。后续就算把 BattleMap 整体替换成 HexMap，大多数枚举和 roster/config 结构仍然可以继续用。

### 枚举

`DS.h` 当前定义了：

- `ECellType`：地块逻辑类型
- `EResourceType`：资源类型
- `EOperatorDeployType`：可部署类型
- `EOperatorAttackType`：攻击类型
- `EOperatorClassType`：职业类型
- `EDeploymentDirection`：部署朝向
- `EDamageType`：伤害类型
- `EGameSpeedState`：游戏速度状态

### **[FOperatorRosterData]**

职责边界：局外全局 roster 条目，保留“名字、类、等级、是否允许参与本场战斗”这类稳定信息。

Properties:

```cpp
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
FName OperatorName;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
TSubclassOf<AOperatorBase> OperatorClass = nullptr;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
int32 OperatorLevel = 1;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
bool bCanJoinBattle = true;
```

读取建议：

- 适合在总览 UI、编队、进入战斗前复制出 `LocalRoster` 时读取
- `bCanJoinBattle` 应在“进入战斗前的编队筛选”阶段读取；`ABattlePlayerController::BeginPlay()` 当前已依赖它过滤不可参战干员

### **[FOperatorLocalRosterData]**

职责边界：局内 roster 条目，补充了实例、是否已部署、部署费用、再部署冷却等运行态信息。

Tech Note：这是 BattleMap 当前局内状态的核心结构；未来 HexMap 仍可能保留其大部分字段，但 `OperatorInstance`、朝向、部署规则等字段可能会被重新解释。

Properties:

```cpp
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
FName OperatorName;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
TSubclassOf<AOperatorBase> OperatorClass = nullptr;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
AOperatorBase* OperatorInstance = nullptr;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
bool bIsDeployed = false;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
EOperatorDeployType DeployType = EOperatorDeployType::Any;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
int32 OperatorLevel = 1;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
int32 CurrentDeploymentCost = 0;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
float RedeployTime = 0.0f;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Roster")
float RedeployCooldown = 0.0f;
```

读取建议：

- 适合在战斗控制器、卡片 UI、部署面板中读取
- 不适合跨关卡长期保存；它本质上是局内临时状态

### **[FOperatorDictionary] (继承自 [FTableRowBase])**

职责边界：干员字典表行结构，用于把 `OperatorName` 映射到 `OperatorClass`。

Properties:

```cpp
UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Operator Config")
FName OperatorName;

UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Operator Config")
TSubclassOf<AOperatorBase> OperatorClass = nullptr;
```

关键资产作用：

- 它不是运行态 roster，而是“配置源”
- 只适合在 `CheckOperators()` 这类装配阶段读取

### **[FLevelLayoutConfig] (继承自 [FTableRowBase])**

职责边界：战斗地图布局配置行结构，用于描述网格尺寸和每一格生成什么地块。

Properties:

```cpp
UPROPERTY(EditAnywhere, BlueprintReadOnly, Category="Level Config")
int32 GridX = 0;

UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Level Config")
int32 GridY = 0;

UPROPERTY(EditAnywhere, BlueprintReadOnly, Category = "Level Config")
TArray<TSubclassOf<ABattleCell>> LayoutData;
```

关键资产作用：

- 当前仅服务 BattleMap 网格生成
- 如果未来切 HexMap，这一结构很可能会被替换为新的六边形地图配置结构

---

## 3. 非总状态层（当前 BattleMap 原型实现）

### 3.1 文件夹：`Source/Arknight/BattleMap/Controller`

#### **[ABattleGameMode] (继承自 [AGameModeBase])**

职责边界：战斗关卡装配器。负责在战斗关卡开始时生成地图、消费待进入关卡 ID、接通镜头。

Tech Note：这是很典型的“场景启动脚本类”。如果未来 HexMap 的战斗入口逻辑完全不同，这个类大概率会重写。

Methods:

```cpp
ABattleGameMode(); // 构造函数，当前未做额外初始化
virtual void BeginPlay() override; // 战斗开场主入口：生成 BattleMapManager -> 消费 PendingBattleID -> 生成地图 -> 对齐相机视角
```

#### **[ABattlePlayerController] (继承自 [APlayerController])**

职责边界：BattleMap 的交互总控。负责输入、点击判定、卡片选择、部署、撤退、费用增长、HUD 创建，以及本场战斗新增资源的运行时统计。

Tech Note：这是当前战斗层最重的类，也是后续最需要拆分的类。HexMap 若重做交互模型，不建议直接沿用其大部分逻辑。

Properties (事件与输入):

```cpp
UPROPERTY(BlueprintAssignable, Category = "Events")
FOnCostChangedSignature OnCostChanged; // 费用变化事件，HUD 和卡片颜色依赖它

UPROPERTY(BlueprintAssignable, Category = "Events")
FOnOperatorCardClicked OnOperatorCardClicked; // 干员卡片选中变化事件

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Input")
UInputMappingContext* BattleMappingContext; // 战斗输入映射资源，应在 BeginPlay/SetupInputComponent 前已配置好

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Input")
UInputAction* ClickAction; // 鼠标点击输入动作，绑定到 OnClickStarted()
```

Properties (UI / 选择 / 局内状态):

```cpp
UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "UI")
TSubclassOf<UBattleHUDWidget> HUDWidgetClass; // 战斗 HUD 资产，应在进入战斗关卡前可用

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "UI")
UBattleHUDWidget* HUDWidgetInstance; // 运行时 HUD 实例

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category="Select")
AOperatorBase* SelectedOperator;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "Select")
FName SelectedOperatorCardName;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "Select")
AResourceCell* SelectedResourceCell;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "Select")
ADeployableCell* SelectedDeployableCell;

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite, Category = "Economy")
int32 CurrentDeploymentCost = 10;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Economy")
UBattleResourceRecordAsset* BattleResourceRecord; // 本场战斗资源统计对象，只记录当前战斗期间新增的资源

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Roster")
TArray<FOperatorLocalRosterData> LocalRoster; // 由 GlobalRoster 复制而来的局内状态表；BeginPlay 时会跳过 bCanJoinBattle == false 的干员
```

Methods:

```cpp
ABattlePlayerController(); // 开启 Tick，允许处理再部署冷却等局内状态
virtual void BeginPlay() override; // 初始化输入模式、复制 LocalRoster、启动自然回费、创建 HUD
virtual void EndPlay(const EEndPlayReason::Type EndPlayReason) override; // 清理自然回费计时器
virtual void Tick(float DeltaSeconds) override; // 逐帧推进所有未部署干员的 RedeployCooldown
virtual void SetupInputComponent() override; // 把 ClickAction 绑定到 OnClickStarted()

UFUNCTION(BlueprintCallable)
void OnClickStarted(); // 当前点击分发中心：区分选中干员、资源地块、可部署格子，以及是否应该弹出部署面板

UFUNCTION(BlueprintCallable)
void OperatorCardChanged(FName OperatorName); // 修改当前选中的卡片名，并广播给所有卡片 UI 与高亮逻辑

UFUNCTION(BlueprintCallable)
void IncreaseCost(int32 Amount); // 增减当前部署费用，并广播给 HUD 与卡片

UFUNCTION(BlueprintCallable, Category = "Economy")
void ResetBattleResourceRecord(); // 创建或清空本场资源统计对象；当前在 BeginPlay() 时自动调用

UFUNCTION(BlueprintCallable, Category = "Economy")
void RecordBattleResourceGain(EResourceType ResourceType, int32 Amount); // 记录本场战斗新增资源；供结算 UI 或战斗结束逻辑读取

UFUNCTION(BlueprintCallable, Category = "Interaction")
void Deploy(FName OperatorName, ADeployableCell* DeployCell, EDeploymentDirection DeployDirection); // 完成局内部署：校验费用 -> SpawnActor -> OnDeployed -> 写回 LocalRoster 与 UI

UFUNCTION(BlueprintCallable, Category = "Interaction")
void Retreat(AOperatorBase* Operat); // 完成撤退：退还部分费用 -> 回收占位格子 -> 销毁实例 -> 启动再部署冷却 -> 刷新卡片
```

当前实现提醒：

- `BeginPlay()` 在复制 `GlobalRoster` 时，会跳过 `FOperatorRosterData::bCanJoinBattle == false` 的条目
- `BattleResourceRecord` 当前是运行时 `NewObject` 创建的数据对象，不是持久化存档资产
- 如果未来结算界面放在切图之后显示，这份本场统计应考虑上移到 `URougeliteRunSubsystem`

#### **[ABattleCameraPawn] (继承自 [APawn])**

职责边界：BattleMap 专用镜头 Pawn。

Tech Note：如果未来 HexMap 使用完全不同的相机语言，这个类可整体替换。

Properties:

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Camera")
USceneComponent* RootComp;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Camera")
USpringArmComponent* SpringArm;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Camera")
UCameraComponent* Camera;
```

Methods:

```cpp
ABattleCameraPawn(); // 创建 Root/SpringArm/Camera，并配置固定视场与无碰撞测试

UFUNCTION(BlueprintCallable, Category = "CameraLogic")
void FrameGridToScreen(int32 GridX, int32 GridY, float CellSize); // 根据矩形网格尺寸粗略计算相机位置与臂长
```

### 3.2 文件夹：`Source/Arknight/BattleMap`

#### **[ABattleMapManager] (继承自 [AActor])**

职责边界：BattleMap 网格地图管理器。

Tech Note：它是“配置表 -> 实际地块 Actor 网格”的生成中介。HexMap 方案如果不再使用方格地图，那么这个类的保留价值会显著下降。

Properties:

```cpp
UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
float CellSize = 100.0f; // 当前方格边长/世界坐标间隔

UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
UDataTable* LevelConfigTable; // 关卡布局表，构造时从 RougeliteSettings 读取

UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
int32 GridX = 0;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
int32 GridY = 0;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
TMap<FIntVector2, ABattleCell*> GridMap; // 逻辑坐标到格子实例的映射
```

关键资产：

```cpp
UDataTable* LevelConfigTable; // 当前 BattleMap 生成的核心配置资产，只应在地图生成或替换格子规则需要时读取
```

Methods:

```cpp
UFUNCTION(BlueprintCallable, Category = "GridLogic")
void GenerateGrid(FName LevelID); // 读取 FLevelLayoutConfig，按 GridX/GridY 与 LayoutData 逐格 Spawn ABattleCell 子类

UFUNCTION(BlueprintCallable, Category = "GridLogic")
ABattleCell* GetCellFromCoordinate(FIntVector2 Coord); // 根据逻辑坐标取地块实例，是索敌、部署、资源退化的基础查询函数

UFUNCTION(BlueprintCallable, Category = "GridLogic")
bool IsCellDeployable(FIntVector2 CellCoord, EOperatorDeployType DeployType); // 判断某坐标是否符合近战/远程部署规则；当前基本未被主流程使用

UFUNCTION(BlueprintCallable, Category = "GridLogic")
void ReplaceCell(FIntVector2 Coord, TSubclassOf<ABattleCell> NewCellClass); // 销毁旧格子并用新类在原坐标重生；资源地块耗尽后依赖它完成退化
```

### 3.3 文件夹：`Source/Arknight/BattleMap/Cell`

#### **[ABattleCell] (继承自 [AActor])**

职责边界：所有方格地块的基类。

Properties:

```cpp
UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Components")
UStaticMeshComponent* CellMeshComponent; // 地块的可视体和根组件

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "CellData")
ECellType LogicalCellType;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "CellData")
FIntVector2 GridCoordinate;
```

Methods:

```cpp
UFUNCTION(BlueprintCallable, Category = "CellLogic")
virtual void InitializeCell(FIntVector2 Coord); // 写入逻辑坐标；所有格子生成后都会调用

ABattleCell(); // 创建 CellMeshComponent 并设为 RootComponent
```

#### **[ADeployableCell] (继承自 [ABattleCell])**

职责边界：可部署地块。

Tech Note：这个类是 BattleMap 特有物。HexMap 未来如果改成“单位占据 hex 单元 + 不同地形规则”，它可能会被新对象取代。

Properties:

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadWrite, Category = "DeployState")
bool bIsOccupied;

UPROPERTY(VisibleAnywhere, BlueprintReadWrite, Category = "DeployState")
AOperatorBase* OccupyingOperator;
```

Methods:

```cpp
UFUNCTION(BlueprintCallable)
void SetOccupyingOperator(AOperatorBase* Operator); // 统一维护 OccupyingOperator 与 bIsOccupied 的同步关系

UFUNCTION(BlueprintCallable)
bool CanDeploy(EOperatorDeployType DeployTpe); // 根据地块类型判断是否允许近战/远程/任意部署，并驱动蓝图视觉提示

UFUNCTION(BlueprintCallable)
void OnOperatorCardClicked(FName OperatorName); // 旧版部署可视反馈入口；当前主流程依赖度较低，可视作遗留辅助逻辑

UFUNCTION(BlueprintImplementableEvent, Category = "CellVisual")
void EnableVisualEffect(); // 蓝图层的“可部署提示”表现

UFUNCTION(BlueprintImplementableEvent, Category = "CellVisual")
void DisableVisualEffect(); // 蓝图层的“取消提示”表现
```

#### **[AResourceCell] (继承自 [ABattleCell], [ITargetableInterface])**

职责边界：资源采集格，同时也是可被索敌与受击的目标。

Tech Note：这是当前 BattleMap 里最有“游戏性”的格子对象，因为它同时接了生命值、资源掉落、退化、索敌接口。

Properties:

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
UHealthComponent* HealthComp; // 生命值组件，伤害与死亡事件都依赖它

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Resource")
EResourceType ResourceType;

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Resource")
int32 DropAmount = 1; // 还能掉落多少次资源

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Resource")
TSubclassOf<ABattleCell> DegradeToCellClass; // 资源采空后替换成什么格子
```

关键资产：

```cpp
TSubclassOf<ABattleCell> DegradeToCellClass; // 资源耗尽时才会读取，用于调用 MapManager->ReplaceCell()
```

Methods:

```cpp
AResourceCell(); // 创建 HealthComp
virtual void BeginPlay() override; // 绑定死亡事件，并把 CurrentHealth 初始化到 MaxHealth

UFUNCTION(BlueprintCallable, Category = "Resource")
void OnCellDepleted(AActor* Killer); // 死亡回调主逻辑：扣减 DropAmount -> 写入 RunSubsystem 全局资源 -> 记录到 BattlePC 的本场资源统计 -> 判断是否退化或回满血继续存在

FIntVector2 GetGridPosition_Implementation() const override; // 向索敌系统暴露自己的网格坐标
bool CanBeTargetedBy_Implementation(AActor* Instigator) const override; // 只有还有掉落次数且存活时，才允许被选为目标
void TakeCombatDamage_Implementation(float DamageAmount, EDamageType DamageType, AActor* DamageInstigator) override; // 把伤害转交给 HealthComp
```

资源流说明：

- 资源实际入账仍通过 `URougeliteRunSubsystem::AddResource()` 完成
- `AResourceCell` 现在会在同一次掉落中额外调用 `ABattlePlayerController::RecordBattleResourceGain()`
- 因此“全局资源背包”和“本场结算统计”当前共享同一触发点，口径一致

### 3.4 文件夹：`Source/Arknight/BattleMap/Data`

#### **[UBattleResourceRecordAsset] (继承自 [UDataAsset])**

职责边界：战斗期间的临时资源统计对象。负责记录本场战斗新增的各类资源数量，供结算 UI 或战斗结束流程读取。

Tech Note：虽然它继承自 `UDataAsset`，但当前不是放在 Content Browser 中长期维护的配置资产，而是由 `ABattlePlayerController` 在运行时动态创建并持有的“轻量数据容器”。

Properties:

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "BattleResult")
TMap<EResourceType, int32> ResourceGains; // 记录当前战斗期间每种资源新增了多少
```

Methods:

```cpp
UFUNCTION(BlueprintCallable, Category = "BattleResult")
void ResetRecord(); // 清空统计，并预置 Wood/Rock/Metal/Grain 四种资源为 0

UFUNCTION(BlueprintCallable, Category = "BattleResult")
void AddResourceGain(EResourceType ResourceType, int32 Amount); // 累加某种资源的本场新增量；仅接受正数

UFUNCTION(BlueprintPure, Category = "BattleResult")
int32 GetResourceGain(EResourceType ResourceType) const; // 读取某种资源在本场战斗中的累计新增量
```

### 3.5 文件夹：`Source/Arknight/BattleMap/UI`

#### **[UBattleHUDWidget] (继承自 [UUserWidget])**

职责边界：BattleMap 的总 HUD 容器。

Tech Note：HexMap 方案如果不再使用“底部卡片 + 右侧详情 + 中心部署弹窗”这一套交互，这个类大概率需要整体重做。

Properties (主要控件):

```cpp
UPROPERTY(meta = (BindWidget))
UCheckBox* PauseCheckBox;

UPROPERTY(meta = (BindWidget))
UCheckBox* SpeedCheckBox;

UPROPERTY(meta = (BindWidget))
UButton* ExitButton;

UPROPERTY(meta = (BindWidget))
UExitWidget* ExitConfirmPanel;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextCost;

UPROPERTY(meta = (BindWidget))
UOperatorDetailWidget* OperatorDetailPanel;

UPROPERTY(meta = (BindWidget))
UCellDetailWidget* CellDetailPanel;

UPROPERTY(meta = (BindWidget))
UDeployWidget* DeployPanel;

UPROPERTY(meta = (BindWidget))
UHorizontalBox* RosterPanel;

UPROPERTY(EditDefaultsOnly)
TSubclassOf<UOperatorCardWidget> OperatorCardWidgetClass; // 卡片 Widget 资产，应在 HUD 构建 roster 时读取
```

Properties (状态):

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Player")
ABattlePlayerController* BattlePC;

UPROPERTY(VisibleAnywhere, BlueprintReadWrite, Category = "GameFlow")
bool bIsGamePaused;

UPROPERTY(VisibleAnywhere, BlueprintReadWrite, Category = "GameFlow")
EGameSpeedState CurrentSpeed;
```

关键资产读取时机：

- `OperatorCardWidgetClass`：仅在 `NativeConstruct()` 动态生成底部 roster 时读取
- `DeployPanel`：仅在点击可部署地块并弹出朝向选择时读取

Methods:

```cpp
virtual void NativeConstruct() override; // 绑定 UI 事件、缓存 BattlePC、初始化底部卡组并折叠各面板

UFUNCTION()
void ChangePauseState(bool bIsChecked); // 改变暂停状态，并直接调用 GameplayStatics 设置游戏暂停

UFUNCTION()
void ChangeSpeedState(bool bIsChecked); // 改变全局时间膨胀，当前只在 1x 与 2x 间切换

UFUNCTION()
void OnExitButtonClicked(); // 打开退出确认面板

UFUNCTION()
void OnCostUpdated(int32 NewCost); // 刷新费用文字

UFUNCTION(BlueprintCallable)
void ShowOperatorSelected(AOperatorBase* SelectedOp); // 只显示干员详情并隐藏其余面板

UFUNCTION(BlueprintCallable)
void ShowOperatorCardSelected(FName OperatorName); // 用“卡片名字”展示干员详情，支持未部署时查看默认数据

UFUNCTION(BlueprintCallable)
void ShowCellSelected(AResourceCell* SelectedCell); // 只显示资源格详情

UFUNCTION(BlueprintCallable)
void HideAllPanels(); // 统一收起干员详情、资源详情、部署面板

UFUNCTION(BlueprintCallable)
void ShowDeployPanel(FName OperatorName, ADeployableCell* DeployableCell); // 把世界坐标投影到屏幕，再把部署朝向面板定位到对应地块上方

UFUNCTION(BlueprintCallable)
void RefreshOperatorCards(); // 遍历底部所有卡片，强制刷新其显隐、费用与冷却状态
```

#### **[UOperatorCardWidget] (继承自 [UUserWidget])**

职责边界：底部单张干员卡片。

Tech Note：当前最脆弱的点是 `OperatorData` 只是指向 `LocalRoster` 某一条目的裸指针。若未来 roster 生命周期变化，这里要改成更稳妥的索引或名字引用。

Properties:

```cpp
UPROPERTY(meta = (BindWidget))
UImage* ImageAvatar;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextCost;

UPROPERTY(meta = (BindWidget))
UProgressBar* ProgressBarCoolDown;

UPROPERTY(meta = (BindWidget))
UButton* HitBox;

const FOperatorLocalRosterData* OperatorData; // 当前卡片绑定的局内 roster 条目
int32 CurrentCost = 0;

UPROPERTY()
bool bIsSelected = false;
```

Methods:

```cpp
virtual void NativeConstruct() override; // 绑定点击事件，并订阅 BattlePC 的卡片选中事件与费用变化事件
virtual void NativeTick(const FGeometry& MyGeometry, float InDeltaTime) override; // 当前主要作为兜底刷新入口；真实状态更新已更多依赖 RefreshCardState()

UFUNCTION(BlueprintCallable)
void RefreshCardState(); // 卡片核心刷新逻辑：决定是否隐藏、是否显示冷却条、是否更新费用文字

UFUNCTION(BlueprintCallable)
void OnCreated(const FOperatorLocalRosterData& OperatorRD); // 首次绑定 roster 数据，并用默认干员类设置头像

UFUNCTION(BlueprintCallable)
void OnClicked(); // 本地切换选中状态，并通过 BattlePC 广播“当前选中的是哪张卡”

UFUNCTION(BlueprintCallable)
void OnClickedCardChanged(FName OperatorName); // 响应全局广播，决定自己是否上浮高亮

UFUNCTION(BlueprintCallable)
void OnCostUpdated(int32 NewCost); // 根据当前可部署费用把卡片变亮或变灰
```

#### **[UOperatorDetailWidget] (继承自 [UUserWidget])**

职责边界：干员详情面板。

Tech Note：这个面板当前能展示两种对象来源：已部署时读 `OperatorInstance`，未部署时读 `OperatorClass` 默认对象。这个模式值得保留，因为很适合将来做统一“单位辞典/预览面板”。

当前实现补充：攻击范围预览现在会固定把中心格画成白色来表示干员自身位置，并给每个范围格加统一 `Padding`。如果 `BaseAttackRange` 中包含 `(0, 0)`，实现层会主动跳过，避免红色攻击格覆盖中心白格。

Properties:

```cpp
UPROPERTY(meta = (BindWidget))
UTextBlock* TextName;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextDescription;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextAtk;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextAspd;

UPROPERTY(meta = (BindWidget))
UImage* ImgAvatar;

UPROPERTY(Transient, meta = (BindWidgetAnim))
UWidgetAnimation* SlideInAnim;

UPROPERTY(meta = (BindWidget))
UGridPanel* GridAttackRange;

UPROPERTY(EditDefaultsOnly)
TSubclassOf<UUserWidget> GridBlockClass; // 小格子预制体，用于绘制攻击范围预览

UPROPERTY(meta = (BindWidget))
UButton* ButtonRetreat;

UPROPERTY()
AOperatorBase* OperatorInfo;
```

Methods:

```cpp
virtual void NativeConstruct() override; // 绑定撤退按钮，并清空当前 OperatorInfo

UFUNCTION(BlueprintCallable, Category = "UI")
void UpdateAndShow(FName OperatorName); // 读取 roster -> 决定使用实例还是默认对象 -> 刷新文本、头像、攻击范围和撤退按钮显隐；攻击范围预览会单独高亮自身中心格

UFUNCTION(BlueprintCallable, Category = "UI")
void HidePanel(); // 简单折叠面板

UFUNCTION(BlueprintCallable)
void OnRetreatClicked(); // 把撤退请求交给 BattlePlayerController，并在 UI 层先收起自己
```

#### **[UDeployWidget] (继承自 [UUserWidget])**

职责边界：部署方向确认面板。

Tech Note：这个面板几乎完全是 BattleMap 交互专用物。HexMap 若不再需要“部署后选择朝向”这一步，它可以直接删除或重做。

Properties:

```cpp
UPROPERTY(meta = (BindWidget))
UButton* Up;

UPROPERTY(meta = (BindWidget))
UButton* Down;

UPROPERTY(meta = (BindWidget))
UButton* Left;

UPROPERTY(meta = (BindWidget))
UButton* Right;

UPROPERTY(meta = (BindWidget))
UImage* ImageAvatar;

UPROPERTY()
FName OperatorName;

UPROPERTY()
ADeployableCell* TargetCell;
```

Methods:

```cpp
virtual void NativeConstruct() override; // 绑定四个方向按钮

UFUNCTION(BlueprintCallable)
void OnUpClicked(); // 以 Up 朝向调用 BattlePC->Deploy()

UFUNCTION(BlueprintCallable)
void OnDownClicked(); // 以 Down 朝向调用 BattlePC->Deploy()

UFUNCTION(BlueprintCallable)
void OnLeftClicked(); // 以 Left 朝向调用 BattlePC->Deploy()

UFUNCTION(BlueprintCallable)
void OnRightClicked(); // 以 Right 朝向调用 BattlePC->Deploy()

UFUNCTION(BlueprintCallable)
void OnShow(FName OpName, ADeployableCell* DeployableCell); // 缓存部署上下文，读取默认干员头像，并显示面板

UFUNCTION(BlueprintCallable)
void HidePanel(); // 折叠面板并清空缓存上下文
```

#### **[UCellDetailWidget] (继承自 [UUserWidget])**

职责边界：资源格详情面板。

Tech Note：它的逻辑相对清晰，但非常 BattleMap 绑定。若 HexMap 后续不再以“点击资源格查看详情”的形式交互，这个面板可整体重构。

Properties:

```cpp
UPROPERTY(meta = (BindWidget))
UTextBlock* TextCellType;

UPROPERTY(meta = (BindWidget))
UImage* ImageGrain;

UPROPERTY(meta = (BindWidget))
UImage* ImageWood;

UPROPERTY(meta = (BindWidget))
UImage* ImageRock;

UPROPERTY(meta = (BindWidget))
UImage* ImageMetal;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextAmount;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextHealth;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextDefense;

UPROPERTY(meta = (BindWidget))
UTextBlock* TextArtsResistance;

UPROPERTY(meta = (BindWidget))
UProgressBar* ProgressBarHealth;

UPROPERTY()
AResourceCell* TargetResource;

UPROPERTY(Transient, meta = (BindWidgetAnim))
UWidgetAnimation* SlideInAnim;
```

Methods:

```cpp
UFUNCTION(BlueprintCallable)
void UpdateAndShow(AResourceCell* ResourceInfo); // 根据资源类型切图标、刷新文本，并弹出详情面板

UFUNCTION(BlueprintCallable)
void HidePanel(); // 折叠面板

virtual void NativeTick(const FGeometry& MyGeometry, float InDeltaTime) override; // 持续刷新血条；目标失效时自动隐藏
```

#### **[UExitWidget] (继承自 [UUserWidget])**

职责边界：退出确认框。

Tech Note：它的保留价值主要在“退出战斗时应走统一结算入口”这一思路，而不是具体 UI 本身。

Properties:

```cpp
UPROPERTY(meta=(BindWidget))
UButton* ConfirmButton;

UPROPERTY(meta = (BindWidget))
UButton* CancelButton;
```

Methods:

```cpp
UFUNCTION(BlueprintCallable)
void Open(); // 显示面板并暂停游戏

virtual void NativeConstruct() override; // 绑定确认/取消按钮并默认折叠

UFUNCTION()
void OnConfirmClicked(); // 触发 RunSubsystem->ConcludeBattle()，把退出请求交给总状态层处理

UFUNCTION()
void OnCancelClicked(); // 收起确认框，并恢复到 HUD 当前记录的暂停状态
```

### 3.6 文件夹：`Source/Arknight/Component`

#### **[UHealthComponent] (继承自 [UActorComponent])**

职责边界：通用生命值与减伤组件。

Tech Note：这类组件值得优先保留，因为不强绑 BattleMap。HexMap 未来大概率仍然需要类似组件。

Properties:

```cpp
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Stats")
float MaxHealth;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly, Category = "Stats")
float CurrentHealth;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Stats")
float Defense;

UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Stats")
float ArtsResistance;

UPROPERTY(BlueprintAssignable, Category = "Events")
FOnHealthZeroSignature OnDeathEvent; // 生命归零事件，资源格等对象通过它响应死亡
```

Methods:

```cpp
UFUNCTION(BlueprintCallable, Category = "Combat")
void ApplyDamage(float BaseDamage, EDamageType DamageType, AActor* Instigator); // 根据物理/法术/真伤规则结算实际伤害，并在归零时广播死亡事件

UFUNCTION(BlueprintCallable, Category = "Stats")
void Heal(float HealAmount); // 回复生命值，但不会超过 MaxHealth
```

#### **[UTargetingComponent] (继承自 [UActorComponent])**

职责边界：索敌规则组件。

Tech Note：这类组件也值得保留。未来无论是方格还是六边形，都可能继续沿用“相对攻击范围 -> 绝对坐标 -> 过滤目标 -> 排序”的思路，只是坐标计算规则会变。

Properties:

```cpp
UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
TArray<FIntVector2> BaseAttackRange; // 以自身为中心的相对攻击范围

UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
float TargetingInterval = 0.1f; // 索敌轮询间隔

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
int32 MaxTargets = 1;

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
float AttackPower;

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
float AttackSpeed;

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
EDamageType DamageType;

UPROPERTY(VisibleAnywhere, BlueprintReadWrite)
TArray<FIntVector2> CurrentAbsoluteRange; // 根据朝向转换后的绝对范围
```

Methods:

```cpp
UFUNCTION(BlueprintCallable, Category = "Targeting|Logic")
void InitializeAbsoluteRange(FIntVector2 Origin, EDeploymentDirection FacingDirection); // 把相对攻击范围按朝向旋转并平移到地图实际坐标

UFUNCTION(BlueprintCallable, Category = "Targeting|Logic")
TArray<TScriptInterface<ITargetableInterface>> PerformGridScan(); // 遍历绝对范围内的格子，过滤无效/不可索敌对象，并按 CalculateTargetScore 排序截断

UFUNCTION(BlueprintNativeEvent, Category = "Targeting|Logic")
float CalculateTargetScore(const TScriptInterface<ITargetableInterface>& Target); // 目标优先级评分函数，当前默认按网格距离计算，后续可被重写
```

### 3.7 文件夹：`Source/Arknight/Operator`

#### **[AOperatorBase] (继承自 [AActor])**

职责边界：干员基类。

Tech Note：这是 BattleMap 中最接近“单位抽象”的类，保留价值高于 UI/地块层。后续 HexMap 很可能仍需要一个单位基类，只是部署、朝向、站位和索敌规则会发生变化。

Properties (表现与战斗基础):

```cpp
UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
FName OperatorName;

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
FText OperatorDescription;

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
UStaticMeshComponent* SpriteComponent; // 当前用 StaticMesh 表现角色；未来若切 Spine2D，可考虑换成更通用的视觉组件

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly, Category = "Visual")
float VisualPlacementOffsetZ = 0.0f; // 自动贴地后的微调高度偏移

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
UTexture2D* AvatarImage; // 卡片、详情、部署面板都依赖它

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
TSubclassOf<ABulletBase> BulletClass; // 仅远程攻击时读取

UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
UTargetingComponent* TargetingComp; // 索敌与攻击参数核心组件
```

Properties (局内状态):

```cpp
UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "State")
EDeploymentDirection FacingDirection;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "State")
FIntVector2 GridLocation;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadWrite, Category = "State")
TArray<TScriptInterface<ITargetableInterface>> Target; // 当前索到的目标集合

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
int32 DeploymentCost;

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
EOperatorDeployType DeployType;

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
float RedeployTime;

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
EOperatorAttackType AttackType;

UPROPERTY(EditDefaultsOnly, BlueprintReadOnly)
EOperatorClassType OperatorClassType;
```

关键资产读取时机：

- `AvatarImage`：卡片 UI、详情面板、部署面板显示时读取
- `BulletClass`：只有 `AttackType == Ranged` 且真正执行远程攻击时才读取
- `VisualPlacementOffsetZ`：只有 `OnDeployed()` 自动贴地时才读取

Methods:

```cpp
UFUNCTION(BlueprintCallable, Category = "Deployment")
void OnDeployed(FIntVector2 Location, EDeploymentDirection Direction, int32 InitialLevel, ADeployableCell* DeployCell); // 部署核心逻辑：写入格子位置与朝向 -> 自动贴地 -> 按等级修正攻击力 -> 初始化索敌范围 -> 启动索敌计时器

UFUNCTION(BlueprintCallable, Category = "Deployment")
void OnRetreat(); // 撤退核心逻辑：清除索敌/攻击计时器并销毁自己

UFUNCTION(BlueprintCallable, Category = "Combat")
void OnTargetingTimerTick(); // 索敌轮询逻辑：刷新 Target；有目标则启动攻击计时器，无目标则停火

UFUNCTION(BlueprintCallable, Category = "Combat")
void ExecuteAttack(); // 出手逻辑：近战直接结算伤害，远程优先生成子弹，失败时回退为直接伤害
```

#### **[ABulletBase] (继承自 [AActor])**

职责边界：远程攻击的投射物。

Tech Note：它依赖目标点直线飞行，没有碰撞盒，也不做路径预测，是当前最轻量的一版原型实现。

Properties:

```cpp
UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
UStaticMeshComponent* MeshComp;

UPROPERTY(EditDefaultsOnly, BlueprintReadWrite)
float FlySpeed;

UPROPERTY(VisibleAnywhere,BlueprintReadWrite)
FVector SelfCoord;

UPROPERTY(VisibleAnywhere, BlueprintReadWrite)
FVector TargetCoord;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
float DamageAmount;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
EDamageType DamageType;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
TScriptInterface<ITargetableInterface> BulletTarget;

UPROPERTY(VisibleAnywhere, BlueprintReadOnly)
AActor* BulletOwner;

UPROPERTY(VisibleDefaultsOnly, BlueprintReadOnly)
FVector Direction;
```

Methods:

```cpp
ABulletBase(); // 创建 MeshComp 并设为根组件

UFUNCTION(BlueprintCallable)
void InitializeBullet(
    FVector InSelfCoord,
    FVector InTargetCoord,
    float InDamageAmount,
    EDamageType InDamageType,
    TScriptInterface<ITargetableInterface> InTarget,
    AActor* InInstigator
); // 初始化起点、终点、伤害和目标，并计算飞行方向

virtual void Tick(float DeltaTime) override; // 沿 Direction 前进；接近目标点后对 BulletTarget 结算伤害并自毁
```

### 3.8 文件夹：`Source/Arknight/Interface`

#### **[UTargetableInterface] / [ITargetableInterface]**

职责边界：统一描述“哪些对象可以被选中为攻击目标，以及如何吃伤害”。

Tech Note：这套接口强烈建议保留。它几乎不依赖 BattleMap 的 UI 和格子表现，是后续敌人、可破坏物、HexMap 单位最适合继续沿用的契约之一。

Methods:

```cpp
UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category = "Targeting")
FIntVector2 GetGridPosition() const; // 向索敌系统提供逻辑坐标

UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category = "Targeting")
bool CanBeTargetedBy(AActor* Instigator) const; // 决定当前对象是否允许被某个施法者选为目标

UFUNCTION(BlueprintNativeEvent, BlueprintCallable, Category = "Combat")
void TakeCombatDamage(float DamageAmount, EDamageType DamageType, AActor* DamageInstigator); // 统一受击入口
```

### 3.9 文件夹：`Source/Arknight/Debug`

#### **[ALvlDebugGameMode] (继承自 [AGameModeBase])**

职责边界：开发时的战斗关卡跳转捷径。

Tech Note：它的价值在于“快速把某个 LevelID 塞进总状态层并跳进战斗图”。如果未来 HexMap 有新的调试入口，这个类的思路可以保留，实现大概率会替换。

Properties:

```cpp
UPROPERTY(EditAnywhere, BlueprintReadWrite, Category = "Debug")
FName LevelID = NAME_None;
```

Methods:

```cpp
virtual void BeginPlay() override; // 若 LevelID 有效，则把它写入 RunSubsystem->PendingBattleLevelID，并跳转到 Lvl_BattleMap
```

### 3.10 文件夹：`Source/Arknight`

#### **[Arknight 模块入口]**

职责边界：注册 Unreal 主模块。

Tech Note：没有具体业务逻辑，但它提醒你“当前项目还没有额外的模块化拆分”。

实现：

```cpp
IMPLEMENT_PRIMARY_GAME_MODULE(FDefaultGameModuleImpl, Arknight, "Arknight"); // 注册项目主模块
```
